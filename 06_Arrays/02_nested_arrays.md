# Whart are nested Arrays 

Nested Arrays in JavaScript are  defined as an Array  within another array.
Nested arrays are also called multi-dimensional arrays.

```js
const arr = [["PERCI", 42], [1]];
```